pre-install: |
  echo "pre-install" >> "$HOME/log"
post-install: |
  echo "post-install" >> "$HOME/log"
  exit 1
pre-uninstall: |
  echo "pre-uninstall" >> "$HOME/log"
post-uninstall: |
  echo "post-uninstall" >> "$HOME/log"
